Stop the monitored application and its daemon, then immediately start it again.

---

## Step 1 — Stop

Invoke `/app:stop` and wait for it to complete.

---

## Step 2 — Start

Once the app is confirmed stopped, invoke `/app:start`.
